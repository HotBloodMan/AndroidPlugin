<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ljt.androidplugin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hook.MainActivity" />
        <activity android:name=".binder.BinderActivity" />
        <activity android:name=".MainControlActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".amspms.AmsPmsActivity" />
        <activity android:name=".interceptactivity.InterceptActivity" />

        <!-- 假Activity -->
        <activity android:name=".interceptactivity.FakeActivity" />
        <!-- <activity android:name=".interceptactivity.TargetActivity"></activity> -->
        <activity android:name=".testmakejar.MakeJarActivity"></activity>
    </application>

</manifest>